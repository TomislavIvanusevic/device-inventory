import mutations from './mutations';
import actions from './actions';

const state = {
  devices: [],
};

const getters = {

};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
